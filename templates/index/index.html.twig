{% extends 'base.html.twig' %}

{% block title %}Bienvenue !{% endblock %}

{% block body %}
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Site de produits</h1>
    <p class="lead">Tous les produits en promo</p>
  </div>
</div>
<div class="container">
  <div class="products-list-container">
    {% for product in products %}
      {{ include('product/promos.html.twig', { product: product }) }}
    {% endfor %}
  </div>
</div>
{% endblock %}
